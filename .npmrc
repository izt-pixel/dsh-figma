# Keep the pnpm content-addressable store inside the project so that all
# installs stay within the workspace and never touch global state.
store-dir=D:/个人文档/figma/.pnpm-store
strict-peer-dependencies=false
